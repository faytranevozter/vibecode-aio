services:
  web:
    build: .
    command: ["httpd", "-f", "-p", "8080", "-h", "/www"]
    ports:
      - "8080"
    volumes:
      - ./workspace-marker.txt:/www/index.html:ro
      - service-data:/data

volumes:
  service-data:
